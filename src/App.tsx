import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const App = () => {
	return (
		<SafeAreaView className="flex-1 w-fit items-center justify-center bg-red-600">
			<Text className="text-[#008000]">Hello App!</Text>
		</SafeAreaView>
	);
};

export default App;
